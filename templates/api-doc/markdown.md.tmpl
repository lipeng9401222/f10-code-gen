# {{TITLE}}

> 生成方式：{{GENERATION_MODE}}
> 生成时间：{{GENERATED_AT}}
{{COVERAGE_NOTE}}

## 基本信息

- **模块**：{{MODULE}}
- **页面 / 应用**：{{APP_NAME}}
- **文档模式**：{{MODE}}
- **请求前缀**：`{{BASE_PATH}}`
- **接口数量**：{{INTERFACE_COUNT}}
- **请求体格式**：JSON 或 `application/x-www-form-urlencoded`（F10 `Utils.request` / `Utils.requestAxios` 兼容）
- **字段来源**：{{FIELD_SOURCE_SUMMARY}}

## 字段覆盖率

- **覆盖率**：{{COVERAGE_PCT}}（{{COVERAGE_WITH_DESC}}/{{COVERAGE_TOTAL}}）
- **说明**：`config` / `intent` 来源字段视为已有业务描述；`mock` / `generated` 来源字段需后端联调确认。

## 接口列表

{{INTERFACE_TABLE_HEADER}}
{{INTERFACE_TABLE}}

## 接口详情

{{INTERFACE_DETAILS}}

---

_修改后端实现时，请同步更新本文档与同名 `*.api.json`。_
